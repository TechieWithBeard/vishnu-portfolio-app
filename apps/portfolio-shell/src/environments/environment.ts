import { AppEnvironment } from './environment.model';

export const environment: AppEnvironment = {
  production: false,
  apiUrl: '/api',
  mlServiceUrl: 'https://vishnu-portfolio-ml.onrender.com',
};
