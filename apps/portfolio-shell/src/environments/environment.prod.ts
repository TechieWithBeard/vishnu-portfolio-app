import { AppEnvironment } from './environment';

export const environment: AppEnvironment = {
  production: true,
  apiUrl: 'https://vishnu-portfolio-api.onrender.com/api',
  enableAnalytics: true,
};
